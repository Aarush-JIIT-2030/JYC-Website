# V18.9.1 Runtime Fix

## Fixed
- Fixed the Vite/Rolldown dependency-scan parse failure in `src/extra-features.jsx`.
- Moved the My JYC agenda `.ics` generation out of the deeply nested JSX click handler into a named `downloadAgenda` handler.
- Kept the same agenda export behaviour while making the JSX parser boundary simpler and more reliable.
- Bumped package and service-worker versions to 18.9.1.

## Verification
- `npm run qa` passes all static, search, SEO, product, code-quality and polish checks.
- Fresh browser/Vite runtime verification should be run with `npm install` and `npm run dev`.
